name: merge-master

on:
  push:
    branches:
      - dev

jobs:
  build_linux:
    uses: ./.github/workflows/test-linux.yml
  marge_master:
    needs: [build_linux]
    runs-on: ubuntu-latest
    steps:
      name: dev -> master
      run : git pull origin dev 